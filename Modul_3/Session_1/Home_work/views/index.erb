<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Items</h1>
    <table> 
        <thead>
            <th>Name</th>
            <th>Price</th>
            <th></th>
            <th>Actions</th>
            <th></th>
        </thead>
        <tbody>
            <% items.each do |item| %>
            <tr>
                <td><%= item.name %></td>
                <td><%= item.price %></td>
                <td><a href="/items/<%= "#{item.id}" %>/show"><button>Show</button></a></td>
                <td><a href="/items/<%= "#{item.id}" %>/edit"><button>Edit</button></a></td>
                <td>
                    <form action="/items/<%= "#{item.id}" %>/delete" method="POST">
                        <input type="hidden" name="_method" value="DELETE">
                        <input type="submit" value="Delete">
                    </form>
                </td>
            </tr>
            <% end %>
        </tbody>
    </table>
<a href="/items/new"><button>New Item</button></a>
</body>
</html>